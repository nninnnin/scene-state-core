{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "dev": {},
    "test": { "dependsOn": ["build"] },
    "typecheck": {},
    "lint": {}
  }
}
